import type { SupabaseClient } from "@supabase/supabase-js";

/**
 * Autenticación desactivada en este prototipo: no se crea cliente Supabase.
 */
export const supabase: SupabaseClient | null = null;
